#include <stdio.h>

int main() {
    long num = 4003600000000014;

    // Get the first two digits
    int firstTwoDigits = (int)num; 


    printf("First two digits as int: %i\n", firstTwoDigits);

    return 0;
}